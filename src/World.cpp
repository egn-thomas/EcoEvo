#include "World.hpp"

// 1. Le constructeur
World::World() {
    // Initialise tes variables ici (ex: spawn de nourriture)
}

// 2. La mise à jour de la logique
void World::step(float deltaTime) {
    // Pour l'instant on ne fait rien, mais la fonction doit exister
}

// 3. Le rendu graphique
void World::draw(sf::RenderWindow& window) {
    // On dessinera les créatures ici plus tard
}